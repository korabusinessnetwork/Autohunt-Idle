// Camada de serviço — crédito de tempo por anúncio recompensado.
//
// Só disponível pra não-assinante. 15min por anúncio, até 2h/dia
// (reseta a cada 24h). Crédito só é aceito via callback validado do
// SDK no servidor — nunca uma rota que confia no client dizendo
// "assisti o anúncio".
//
// TODO (Fase 1 — build): integrar SDK de anúncio do portal escolhido.
export {};
