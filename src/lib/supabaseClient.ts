// Instância única do client Supabase — nenhum outro arquivo do projeto
// deve chamar createClient() além deste.
//
// Chaves via import.meta.env.VITE_* (nunca hardcoded — ver CLAUDE.md).
//
// TODO (Fase 1 — build): implementar quando o projeto React/Vite for criado.
export {};
